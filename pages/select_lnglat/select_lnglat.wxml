<button type="primary" bindtap="change">confirm</button>

<map id="map4select"
 bindtap="change"
 longitude="{{longitude}}" latitude="{{latitude}}" 
 markers="{{markers}}"
 scale="20" 
 controls="{{controls}}">
</map>