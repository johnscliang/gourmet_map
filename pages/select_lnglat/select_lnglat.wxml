
<map id="map4select"
 longitude="{{longitude}}" latitude="{{latitude}}" 
 markers="{{markers}}"
 scale="20" 
 controls="{{controls}}">
</map>

<button type="primary" bindtap="getLngLat">确认</button>